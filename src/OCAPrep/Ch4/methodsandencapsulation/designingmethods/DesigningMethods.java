package OCAPrep.Ch4.methodsandencapsulation;

public class DesigningMethods {
        public final void nap (int minutes) throws InterruptedException {
            /*
            What is required for method declaration? 4 things
            public = Access modifier (not required)
            final = optional specifier (not required)
            void = return type (required)
            nap = method name (required)
            (int minutes) = parameters (required, but can be empty)
            throws = exception (not required)
            {//take a nap} = method body (required, but can be empty)
            //take a nap



             */
        }
}
